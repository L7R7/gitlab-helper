image: fpco/stack-build:lts-22.22

variables:
  STACK_ROOT: "${CI_PROJECT_DIR}/.stack-root"

cache:
  paths:
    - .stack
    - .stack-work
    - .stack-root
    - target

build:
  stage: build
  script:
    - stack ghc test
